# Competition template

Copy via `maximus new <name>`, then implement `src/train.py`, `src/eval.py`,
and `src/submit.py` for your contest.

Write `artifacts/metrics.json` with a numeric `metric` field after eval.
