# toy_binary

Synthetic binary classification used to prove the Maximus train/eval/log loop
without GPUs or contest signup.

Metric: ROC-AUC on a held-out split (written to `artifacts/metrics.json`).
